import 'package:flutter/material.dart';

class WomenScreen extends StatelessWidget {
  const WomenScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Women Collection', style: TextStyle(fontSize: 24)),
    );
  }
}
